{
  "name": "template",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx nuxt dev --cwd app",
    "generate": "npx nuxt generate --cwd app",
    "preview": "npx serve ./app/.output/public",
    "clean": "npx nuxt cleanup --cwd app"
  },
  "dependencies": {
    "dompurify": "^3.4.12"
  },
  "devDependencies": {
    "js-beautify": "^2.0.3",
    "nuxt": "^3.15.0",
    "sass": "^1.82.0",
    "serve": "^14.2.4",
    "sharp": "^0.35.3"
  }
}
